#include<stdio.h>

struct node{
    int vertex;
    struct node *next;
};
struct node *A[10];

void init(struct node *A[],int n)
{
    int i;
    for()
}

int main()
{
    int n;
    printf("Enter no.of vertices:");
    scanf("%d",&n);
    init(A,n);
    create(A);
    BFS(A,n);
}